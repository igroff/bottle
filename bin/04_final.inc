# vim: ft=sh
# this is a helper include to allow us to dump interesting config information 
# after all the includes have been processed, so it should always be the last
# include

#debug "SUBSCRIBER_REPO_DIR:" ${SUBSCRIBER_REPO_DIR}
